/**
 * Convert .fodt documents into simple HTML
 *
 * @param  {String} html
 * @return {String}
 */
module.exports = {
  convert: function(source, dest) {
    source = source | 'fodt/';
    dest = dest | 'dest/';






  }
};